## Generating a Certificate

Generate a signing certificate by running:
```
adt -certificate -cn "protanki.android" 2048-RSA android_certificate.p12 <password_here>
```

For more information about the `adt -certificate` command see the [AIR SDK docs](https://airsdk.dev/docs/building/air-developer-tool/commands/certificate).
